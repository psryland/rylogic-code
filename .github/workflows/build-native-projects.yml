name: Build Native Projects

permissions:
  contents: read

on:
  push:
    branches: [main]
    paths:
      - '**/*.cpp'
      - '**/*.c'
      - '**/*.h'
      - '**/*.hpp'
      - '**/*.vcxproj'
      - '**/*.vcxproj.filters'
      - '**/*.props'
      - '**/*.targets'
      - 'include/**'
      - 'sdk/**'
      - 'Directory.Build.props'
      - '.github/workflows/build-native-projects.yml'
  pull_request:
    branches: [main]
    paths:
      - '**/*.cpp'
      - '**/*.c'
      - '**/*.h'
      - '**/*.hpp'
      - '**/*.vcxproj'
      - '**/*.vcxproj.filters'
      - '**/*.props'
      - '**/*.targets'
      - 'include/**'
      - 'sdk/**'
      - 'Directory.Build.props'
      - '.github/workflows/build-native-projects.yml'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install dotnet-script
        run: dotnet tool install -g dotnet-script

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      # The repo nuget.config references a local package source that only
      # exists on the dev machine. Replace it with just nuget.org for CI.
      # dotnet-script also uses this config when restoring its internal project.
      - name: Configure NuGet for CI
        shell: pwsh
        run: |
          @'
          <?xml version="1.0" encoding="utf-8"?>
          <configuration>
            <config>
              <add key="repositoryPath" value="packages" />
            </config>
            <packageRestore>
              <add key="enabled" value="true" />
              <add key="automatic" value="true" />
            </packageRestore>
            <packageSources>
              <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
            </packageSources>
          </configuration>
          '@ | Set-Content -Path nuget.config

      # Fetch external SDK dependencies that are git-ignored in the repo.
      # Each _get.csx script clones/downloads its SDK if not already present.
      - name: Fetch SDK dependencies
        shell: pwsh
        run: |
          $scripts = Get-ChildItem -Path sdk -Filter _get.csx -Recurse
          foreach ($script in $scripts) {
            Write-Host "Running $($script.FullName)..."
            dotnet-script $script.FullName
          }

      # Remove C# and VSIX projects, keeping only native (.vcxproj) projects.
      # Also remove test/sandbox vcxprojs - this workflow is a "does it compile"
      # smoke test for the production libraries, not a test runner.
      - name: Remove non-native and test projects from solution
        shell: pwsh
        run: |
          $csprojs = Select-String -Path Rylogic.sln -Pattern '\.csproj"' |
            ForEach-Object { if ($_ -match '"([^"]+\.csproj)"') { $matches[1] } }
          if ($csprojs) {
            dotnet sln Rylogic.sln remove @csprojs
          }
          $testprojs = Select-String -Path Rylogic.sln -Pattern '"[^"]*projects[\\/]tests[\\/][^"]+\.vcxproj"' |
            ForEach-Object { if ($_ -match '"([^"]+\.vcxproj)"') { $matches[1] } }
          if ($testprojs) {
            dotnet sln Rylogic.sln remove @testprojs
          }

      # Restore NuGet packages for native projects.
      # All native vcxproj's use packages.config (PackageReference is broken for C++
      # projects - see build/targets/WinPixEventRuntime.targets). Running
      # `msbuild /t:restore` here triggers the legacy Microsoft.NuGet.targets which
      # writes a project.assets.json that then causes "Sequence contains no elements"
      # when MSBuild tries to consume it during the build. `nuget restore` alone is
      # the correct path for packages.config-based vcxproj's.
      - name: Restore NuGet packages
        shell: pwsh
        run: nuget restore Rylogic.sln

      # Override PlatformToolset since CI has VS 2022 (v143) but some
      # projects target older toolsets not available on the runner.
      - name: Build
        run: msbuild Rylogic.sln /p:Configuration=Release /p:Platform=x64 /p:PlatformToolset=v143 /m
