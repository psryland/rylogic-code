<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!--
  Globals
  -->
  <PropertyGroup Label="Globals">
    <ConfigurationType>Application</ConfigurationType>
    <VCProjectVersion>16.0</VCProjectVersion>
    <ProjectGuid>{F000F1BC-2B9A-4F9F-8E43-6A0DC9A27EB4}</ProjectGuid>
    <RootNamespace>code_sync</RootNamespace>
    <ProjectName>code_sync</ProjectName>
    <DisableFastUpToDateCheck>true</DisableFastUpToDateCheck>
    <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
    <RepositoryToolName>code_sync</RepositoryToolName>
    <RunCodeSync>false</RunCodeSync>
  </PropertyGroup>
  <!--
  Property Sheets
  -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
  <Import Project="$(PropsPath)build_configurations.props" />
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />
  <Import Project="$(PropsPath)app.props" />
  <!--
  Build options
  -->
  <ItemDefinitionGroup>
    <ClCompile>
      <EnableModules>false</EnableModules>
      <LanguageStandard>stdcpplatest</LanguageStandard>
      <AdditionalIncludeDirectories>
        ;$(ProjectDir)
        ;%(AdditionalIncludeDirectories)
      </AdditionalIncludeDirectories>
    </ClCompile>
    <Link>
      <SubSystem>Console</SubSystem>
    </Link>
  </ItemDefinitionGroup>
  <!--
  Files
  -->
  <ItemGroup>
    <ClCompile Include="src\main.cpp" />
    <ClInclude Include="src\forward.h" />
    <ClInclude Include="src\code_sync.h" />
  </ItemGroup>
  <!--
  Deploy
  -->
  <Target Name="Deploy" DependsOnTargets="Build">
    <Error Condition="'$(Configuration)' != 'Release'" Text="Manual code_sync deployment requires Configuration=Release." />
    <MakeDir Directories="$(RepositoryToolsDirectory)\code_sync" />
    <Copy SourceFiles="$(TargetPath)" DestinationFiles="$(RepositoryToolsDirectory)\code_sync\code_sync.exe" />
    <Message Text="Manually deployed Release code_sync.exe" Importance="high" />
  </Target>
  <!--
  Targets
  -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
