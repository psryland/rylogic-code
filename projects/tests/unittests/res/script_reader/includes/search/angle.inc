// Included via an angle-bracket '#include <angle.inc>' from 'canonical.utf8.txt',
// resolved only through the search path added by the preceding '#include_path'
// directive (never through the including file's own directory).
*FromAngle "angle include content"
