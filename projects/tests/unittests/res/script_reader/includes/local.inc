// Included via a quoted, local-dir-relative '#include "includes/local.inc"'
// from 'canonical.utf8.txt'. Both readers resolve this against the including
// file's own directory.
*FromLocal "local include content"
#include "nested/child.inc"
