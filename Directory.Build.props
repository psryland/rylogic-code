<Project>
	<!--
	This is for all C# and C++ projects remember, not just library assemblies
	-->

	<!-- Project variable defines -->
	<PropertyGroup>
		<RylogicRoot>$(MSBuildThisFileDirectory)</RylogicRoot>
		<ScriptPath>$(MSBuildThisFileDirectory)script\</ScriptPath>
		<ToolsPath>$(MSBuildThisFileDirectory)tools\</ToolsPath>
		<PropsPath>$(MSBuildThisFileDirectory)build\props\</PropsPath>
		<TargetsPath>$(MSBuildThisFileDirectory)build\targets\</TargetsPath>
		<PackagesPath>$(MSBuildThisFileDirectory)packages\</PackagesPath>
		<DeployPath>$(MSBuildThisFileDirectory)lib\</DeployPath>
		<SDKPath>$(MSBuildThisFileDirectory)sdk\</SDKPath>
	</PropertyGroup>

	<Import Project="$(MSBuildThisFileDirectory)Directory.Build.user.props" Condition="Exists('$(MSBuildThisFileDirectory)Directory.Build.user.props')" />

	<PropertyGroup>
		<RylogicGeneratePackageOnBuild Condition="'$(RylogicGeneratePackageOnBuild)' == ''">true</RylogicGeneratePackageOnBuild>
		<RylogicLibraryVersion>2.1.4</RylogicLibraryVersion>
	</PropertyGroup>

	<!-- C# -->
	<PropertyGroup Condition="'$(MSBuildProjectExtension)' == '.csproj'">
		<PrimaryTarget>net10.0-windows</PrimaryTarget>
		<TargetFrameworks>$(PrimaryTarget);net481</TargetFrameworks>
		<WarningsAsErrors>Nullable</WarningsAsErrors>
		<GeneratePackageOnBuild>False</GeneratePackageOnBuild>
		<RunAnalyzersDuringBuild>False</RunAnalyzersDuringBuild>
		<SatelliteResourceLanguages>en</SatelliteResourceLanguages>
		<RestorePackagesWithLockFile>False</RestorePackagesWithLockFile>
		<RestorePackagesPath>$(PackagesPath)</RestorePackagesPath>
		<TreatWarningsAsErrors>false</TreatWarningsAsErrors>
		<LangVersion>latest</LangVersion>
		<WarningLevel>9999</WarningLevel>
		<Nullable>Enable</Nullable>
		<NuGetAudit>false</NuGetAudit>

		<!-- C# Assembly properties -->
		<Authors>P Ryland</Authors>
		<Company>Rylogic</Company>
		<PackageLicenseExpression>MIT</PackageLicenseExpression>
		<PackageReadmeFile>Readme.md</PackageReadmeFile>
		<Description>A toolbox of extension methods, data structures, and helper classes used in tools created by Rylogic.</Description>
		<Copyright>Copyright (c) Rylogic 2007</Copyright>
		<!-- Deterministic builds -->
		<Deterministic>true</Deterministic>
		<ContinuousIntegrationBuild>true</ContinuousIntegrationBuild>
		<EmbedUntrackedSources>true</EmbedUntrackedSources>
		<RepositoryUrl>https://github.com/psryland/rylogic-code</RepositoryUrl>
		<RepositoryType>git</RepositoryType>

		<!-- Default build configuration -->
		<Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
		<Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
		<SubDir>$(MSBuildProjectName)\$(TargetFramework)\$(Platform)\$(Configuration)</SubDir>
	</PropertyGroup>
	<Import Project="$(RylogicRoot)lib\packages\local\versions\Rylogic.Native.props" Condition="'$(MSBuildProjectExtension)' == '.csproj' AND '$(Configuration)' == 'Debug' AND Exists('$(RylogicRoot)lib\packages\local\versions\Rylogic.Native.props')" />
	<PropertyGroup Condition="'$(MSBuildProjectExtension)' == '.csproj'">
		<RylogicNativePackageVersion Condition="'$(RylogicNativePackageVersion)' == ''">$(RylogicLibraryVersion)</RylogicNativePackageVersion>
	</PropertyGroup>

	<!-- C++ -->
	<PropertyGroup Condition="'$(MSBuildProjectExtension)' == '.vcxproj'">
		<VCProjectVersion>16.0</VCProjectVersion>
		<PlatformToolset>v145</PlatformToolset>
		<WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
		<RestorePackagesPath>$(PackagesPath)</RestorePackagesPath>
		<VCToolsVersion />

		<!-- These lines disable problematic imports -->
		<ImportProjectExtensionProps>false</ImportProjectExtensionProps>
		<ImportProjectExtensionTargets>false</ImportProjectExtensionTargets>
		<ImportUserLocationsByWildcardBeforeMicrosoftCommonProps>false</ImportUserLocationsByWildcardBeforeMicrosoftCommonProps>
		<ImportUserLocationsByWildcardAfterMicrosoftCommonProps>false</ImportUserLocationsByWildcardAfterMicrosoftCommonProps>

		<!-- Default build configuration -->
		<PlatformTarget Condition=" '$(PlatformTarget)' == '' ">x64</PlatformTarget>
		<ConfigurationName Condition=" '$(ConfigurationName)' == '' ">Debug</ConfigurationName>
		<SubDir>$(MSBuildProjectName)\$(PlatformTarget)\$(Configuration)</SubDir>
		<OutDir>$(RylogicRoot)obj\$(SubDir)\</OutDir>
		<IntDir>$(RylogicRoot)obj\$(SubDir)\</IntDir>
	</PropertyGroup>

</Project>
